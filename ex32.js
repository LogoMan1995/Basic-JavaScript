// 32. Создайте функцию addFiveбез аргументов. Эта функция добавляет 5 к sumпеременной, но ее возвращаемое значение равно undefined.

let sum = 3;

function addFive() {
  sum = sum + 5;
}

addFive();
