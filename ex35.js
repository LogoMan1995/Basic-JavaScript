// 35. Добавьте оператор равенства в указанную строку, чтобы функция возвращала строку Equal, val эквивалентную 12.

function testEqual(val) {
    if (val == 12) { 
      return "Equal";
    }
    return "Not Equal";
  }
  
  testEqual(12);


