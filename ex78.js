// 78. Используйте этот метод для генерации и возврата случайного целого числа в диапазоне от 0 до 9.

function randomWholeNum() {
    console.log( Math.floor(Math.random()*10));
  }
  
  randomWholeNum()

