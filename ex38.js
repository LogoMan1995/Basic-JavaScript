// 38. !=Добавьте в оператор оператор неравенства if, чтобы функция возвращала строку, Not Equal не val эквивалентную 99.

function testNotEqual(val) {
    if (val != 99) { 
      return "Not Equal";
    }
    return "Equal";
  }
  
  testNotEqual(10);


